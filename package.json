{
  "name": "groupus",
  "author": "Thomas Breitner",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.11.0",
    "bootstrap": "^5.3.0",
    "bootstrap-icons": "^1.11.3",
    "htmx.org": "^1.9.10",
    "parcel": "^2.11.0",
    "sass": "^1.70.0",
    "sortablejs": "^1.15.2"
  },
  "config": {
    "assets_src": "./assets",
    "assets_dist": "./static"
  },
  "scripts": {
    "build": "parcel build $npm_package_config_assets_src/app.js --dist-dir $npm_package_config_assets_dist",
    "watch": "parcel watch $npm_package_config_assets_src/app.js --dist-dir $npm_package_config_assets_dist"
  }
}
